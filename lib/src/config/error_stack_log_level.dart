/// The level of logging to be displayed when an error occurs.
enum ErrorStackLogLevel {
  /// Minimal logging - only shows basic error information.
  minimal,

  /// Verbose logging - includes full stack trace and additional details.
  verbose,
}
