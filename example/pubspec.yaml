name: error_stack_example
description: Learn more here https://nylo.dev.

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.1
  error_stack:
    path: ../
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true